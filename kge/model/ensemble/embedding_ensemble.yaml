import: [lookup_embedder]

embedding_ensemble:
  class_name: EmbeddingEnsemble
  entity_embedder:
    type: lookup_embedder
    +++: +++
  relation_embedder:
    type: lookup_embedder
    +++: +++
  submodels: [transe]
  dim_reduction: autoencoder
  l_norm: 1.


