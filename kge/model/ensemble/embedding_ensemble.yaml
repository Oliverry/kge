import: [lookup_embedder]

scoring_ensemble:
  class_name: EmbeddingEnsemble
  entity_embedder:
    type: lookup_embedder
    +++: +++
  relation_embedder:
    type: lookup_embedder
    +++: +++
  submodels: [transe]
  evaluator: platt
  l_norm: 1.
  plattscaling:
    num_models: 2

