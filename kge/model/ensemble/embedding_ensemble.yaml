import: [dim_reduction, embedding_evaluator]

embedding_ensemble:
  class_name: EmbeddingEnsemble
  submodels: [transe, complex]
  dim_reduction: autoencoder
  evaluator: kge_adapter
  entities:
    source_dim: 128
    +++: +++
  relations:
    source_dim: 128
    +++: +++